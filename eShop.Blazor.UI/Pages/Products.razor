@page "/products"
@inherits ProductsBase
@inject NavigationManager NavigationManager

<h3>Products List</h3>
<div class="row">
    <div class="col-6 col-md-2">
        <a href="/createProduct"><button class="btn btn-outline-primary">Add Product</button></a>
    </div>
</div>
<table class="table table-responsive-sm">
    <thead>
        <tr>
            <th>Code</th>
            <th>Name</th>
            <th>Price</th>
            <th>Stock</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    @if (Products != null)
    {
        <tbody>
            @foreach (var product in Products)
            {
                var Value = product.Id;
                var url = NavigationManager.ToAbsoluteUri($"UpdateProduct/{Value}");
                <tr>
                    <td>@product.Code</td>
                    <td>@product.Name</td>
                    <td>@product.Price</td>
                    <td>@product.StockQty</td>
                    <td><span class="fas fa-trash-alt" @onclick="async () => await DeleteProduct(product.Id)"></span></td>
                    <td><a href="@url"><button class="btn btn-primary">Edit</button></a></td>
                </tr>

            }
        </tbody>
    }
</table>
